{
  "name": "runixo-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src",
    "preview": "vitepress preview src",
    "deploy": "npm run build && wrangler pages deploy src/.vitepress/dist --project-name=runixo"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "wrangler": "^3.99.0",
    "@cloudflare/workers-types": "^4.20250205.0"
  }
}
